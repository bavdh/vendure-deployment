apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: vendure
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vendure
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
